1. Contact-Form from front website connects to the database, and backoffice UI.
2. Also for the talent pipeline, we need to have a nav item in the footer or nav bar a form for job applicants to fill out and then it connects to the talent pipeline data base. Also currently we are using a third party url from 4geeks to access the talent pipeline databse. Id like to move away from it and store the applicant in our own database/ architecture. The third party url was only temporary.
3. Also we need to really rethink the current layout of backoffice UI. Now that agents are comming into the mix.
   - Partial progress (final-polish 2.5): the top-center Business/Technical view toggle was removed and
     the sidebar is now the sole navigation; the home always shows the live Operations Overview. This is
     an incremental cleanup, not the full information-architecture restructure, which remains open.

# `resources/`

Non-code shared resources — schemas, templates, config assets.

Distinct from `packages/shared/`, which holds versioned TypeScript code used across the monorepo.
This folder is for anything that doesn't belong in an app or a package: reference files, config templates, reusable schemas.

- Subfolders should each have their own README explaining what they contain.


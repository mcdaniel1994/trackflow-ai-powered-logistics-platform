# `skills/research/`

Scaffolding folder for reusable research skill assets.

## Purpose

Reserved for research workflows, prompts, and supporting assets used by agents when gathering information, summarizing sources, or preparing briefings.

## Current State

Placeholder — `examples/` and `templates/` exist but no active `SKILL.md` is defined yet.

## When Implementing

Add as needed:

- `SKILL.md` with clear use criteria and workflow steps
- `templates/` for reusable prompt or report shapes
- `examples/` for sample inputs and outputs
- `resources/` for reference material
- `scripts/` for helper automation

Keep the structure aligned with `skills/_template/` and `skills/README.md`.

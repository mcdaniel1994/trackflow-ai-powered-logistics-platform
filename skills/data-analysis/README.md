# `skills/data-analysis/`

Scaffolding folder for reusable data analysis skill assets.

## Purpose

Reserved for data analysis workflows, prompts, and supporting assets used by agents when exploring datasets, computing statistics, or producing analytical reports.

## Current State

Placeholder — `resources/` and `scripts/` exist but no active `SKILL.md` is defined yet.

## When Implementing

Add as needed:

- `SKILL.md` with clear use criteria and workflow steps
- `resources/` for reference material (datasets, schemas, checklists)
- `scripts/` for helper automation
- `examples/` for sample inputs and outputs

Keep the structure aligned with `skills/_template/` and `skills/README.md`.
